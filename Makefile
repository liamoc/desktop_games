upto11::
	cd upto11 && cargo bundle --release
	butler push target/release/bundle/osx/Up\ to\ 11.app liamoc/mep-up-to-11:osx

allhands::
	cd allhands && cargo bundle --release
	butler push target/release/bundle/osx/All\ Hands.app liamoc/mep-all-hands:osx
archaeologist::
	cd archaeologist && cargo bundle --release
	butler push target/release/bundle/osx/Archaeologist.app liamoc/mep-archaeologist:osx

solitude::
	cd solitude && cargo bundle --release
	butler push target/release/bundle/osx/Solitude.app liamoc/mep-solitude:osx

soyuz::
	cd soyuz && cargo bundle --release
	butler push target/release/bundle/osx/Soyuz.app liamoc/mep-soyuz:osx

corbynball::
	cd corbynball && cargo bundle --release
	butler push target/release/bundle/osx/Corbynball.app liamoc/mep-corbynball:osx

cylons::
	cd cylons && cargo bundle --release
	butler push target/release/bundle/osx/Cylons.app liamoc/mep-cylons:osx

taipan::
	cd taipan && cargo bundle --release
	butler push target/release/bundle/osx/Taipan.app liamoc/mep-taipan:osx

vermin::
	cd vermin && cargo bundle --release
	butler push target/release/bundle/osx/Vermin\'s\ Vengeance.app liamoc/mep-vermins-vengeance:osx

gatchi::
	cd gatchi && cargo bundle --release
	butler push target/release/bundle/osx/Gatchi.app liamoc/mep-gatchi:osx

vexation::
	cd vexation && cargo bundle --release
	butler push target/release/bundle/osx/Vexation.app liamoc/mep-vexation:osx

jackpot::
	cd jackpot && cargo bundle --release
	butler push target/release/bundle/osx/Jackpot.app liamoc/mep-jackpot:osx
